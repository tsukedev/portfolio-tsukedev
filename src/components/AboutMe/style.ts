import { styled } from "@/styles/stitches.config";

export const AboutMe = styled("section", {
  background: "$grey0",
  padding: "$section 0",
  "@mobile": {
    padding: "$sectionMobile 0",
  },
});
